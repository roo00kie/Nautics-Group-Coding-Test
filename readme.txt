Nautics Group coding test.

Data stored in dataHandling_dataC.data[] are the data occured in the data stream. All data values with MSB == 1 indicate there are adjacent duplicates.

dataHandling_dataC.duplicateCount[] stores the duplicated counts.
